%h1 Register Form

-# frozen_string_literal: true
= simple_form_for @user, :url => register_path do |f|
  = f.error_notification
  = f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present?

  .form-inputs
    = f.input :email,class:'col-12 mt-3'
    = f.input :name,class:'mt-3'
    = f.input :password,class:'mt-3', :input_html => {:minlength => 8 }
    = f.input :password_confirmation, required:true,class:'mt-3'

  .form-actions
    = f.button :submit,'Create User',class:'btn btn-success mt-3',commit: 'Create_user'
    = link_to 'Cancel', login_path,class:"btn btn-outline-danger mt-3"
